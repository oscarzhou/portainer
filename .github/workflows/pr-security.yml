name: Code Security Scan

on: 
  push:
    branches:
      - develop 
  pull_request:
    branches:
      - develop
      - security-scan-ee
  schedule:
    - cron: '30 * * * *'
    
jobs:
  client-dependencies:
    name: Client dependency check
    runs-on: ubuntu-latest
    outputs:
      js: ${{ steps.set-matrix.outputs.js_result }}
      jsdiff: ${{ steps.set-diff-matrix.outputs.js_diff_result }}
    if: github.ref == 'refs/heads/develop' # Temporary disable client check
    steps:
      - uses: actions/checkout@master

      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/node@master
        continue-on-error: true # To make sure that artifact upload gets called
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --fail-on=upgradable
          json: true

      - name: Upload js security scan result as artifact on develop
        uses: actions/upload-artifact@v3
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        with:
          name: js-security-scan-develop-result
          path: snyk.json

      - name: Upload js security scan result as artifact
        uses: actions/upload-artifact@v3
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        with:
          name: js-security-scan-feat-result
          path: snyk.json

      - name: Analyse the js result
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        id: set-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 summary -report-type=snyk -path="/data/snyk.json" -output-type=matrix)
          echo "::set-output name=js_result::${result}"

      - name: Download artifacts from develop branch
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          mv ./snyk.json ./js-snyk-feature.json
          (gh run download -n js-security-scan-develop-result -R ${{ github.repository }} 2>&1 >/dev/null) || :
          if [[ -e ./snyk.json ]]; then
            mv ./snyk.json ./js-snyk-develop.json
          else
            echo "null" > ./js-snyk-develop.json
          fi

      - name: Analyse the js diff result
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        id: set-diff-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 diff -report-type=snyk -path="/data/js-snyk-feature.json" -compare-to="./data/js-snyk-develop.json" -output-type=matrix)
          echo "::set-output name=js_diff_result::${result}"

      - name: Dump context json
        id: github_context_step
        run: echo '${{ toJSON(github) }}'

  server-dependencies:
    name: Server dependency check
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' # Temporary disable client check
    outputs:
      go: ${{ steps.set-matrix.outputs.go_result }}
      godiff: ${{ steps.set-diff-matrix.outputs.go_diff_result }}
    defaults:
      run:
        working-directory: ./api
    steps:
      - uses: actions/checkout@master

      - name: Download dependencies
        run: go get -v -d

      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/golang@master
        continue-on-error: true # To make sure that artifact upload gets called
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --file=./api/go.mod
          json: true

      - name: Upload go security scan result as artifact on develop
        uses: actions/upload-artifact@v3
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        with:
          name: go-security-scan-develop-result
          path: snyk.json

      - name: Upload go security scan result as artifact
        uses: actions/upload-artifact@v3
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        with:
          name: go-security-scan-feat-result
          path: snyk.json

      - name: Analyse the go result
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        id: set-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 summary -report-type=snyk -path="/data/snyk.json" -output-type=matrix)
          echo "::set-output name=go_result::${result}"

      - name: Download artifacts from develop branch
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          mv ./snyk.json ./go-snyk-feature.json
          (gh run download -n go-security-scan-develop-result -R ${{ github.repository }} 2>&1 >/dev/null) || :
          if [[ -e ./snyk.json ]]; then
            mv ./snyk.json ./go-snyk-develop.json
          else
            echo "null" > ./go-snyk-develop.json
          fi

      - name: Analyse the go diff result
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        id: set-diff-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 diff -report-type=snyk -path="/data/go-snyk-feature.json" -compare-to="./data/go-snyk-develop.json" -output-type=matrix)
          echo "::set-output name=go_diff_result::${result}"

  build_image:
    name: Build docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Use golang 1.17.x
        uses: actions/setup-go@v3
        with:
          go-version: '>=1.17.0'

      - name: Use Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: Install packages and build
        run: yarn install && yarn build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          file: build/linux/Dockerfile
          tags: trivy-portainer:${{ github.sha }}
          outputs: type=docker,dest=/tmp/trivy-portainer-image.tar

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: image-artifact
          path: /tmp/trivy-portainer-image

  image-vulnerability:
    name: Image vulnerability check
    needs: [build_image]
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.set-matrix.outputs.image_result }}
      imagediff: ${{ steps.set-diff-matrix.outputs.image_diff_result }}
    steps:
      - name: Checkout code
        uses: actions/checkout@master

      - name: Download image artifact
        uses: actions/download-artifact@v3
        with:  
          name: image-artifact
          path: /tmp

      - name: Load docker image
        run: |
          docker load --input /tmp/trivy-portainer-image

      - name: Run Trivy vulnerability scanner
        uses: docker://docker.io/aquasec/trivy:latest
        continue-on-error: true 
        with:
          args: image --ignore-unfixed=true --vuln-type="os,library" --severity="CRITICAL,HIGH,MEDIUM" --exit-code=1 --format="json" --output="image-trivy.json" --no-progress trivy-portainer:${{ github.sha }}
          
      - name: Upload image security scan result as artifact on develop
        uses: actions/upload-artifact@v3
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        with:
          name: image-security-scan-develop-result
          path: image-trivy.json

      - name: Upload image security scan result as artifact
        uses: actions/upload-artifact@v3
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        with:
          name: image-security-scan-feat-result
          path: image-trivy.json

      - name: Analyse the trivy result
        if: >-
          github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee'
        id: set-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 summary -report-type=trivy -path="/data/image-trivy.json" -output-type=matrix)
          echo "::set-output name=image_result::${result}"

      - name: Download artifacts from develop branch
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          mv ./image-trivy.json ./image-trivy-feature.json
          downloadError=$(gh run download -n image-security-scan-develop-result -R ${{ github.repository }} 2>&1 >/dev/null) || :
          if [[ -n ${downloadError} ]]; then
            echo "null" > ./image-trivy-develop.json
          else
            mv ./snyk.json ./image-trivy-develop.json
          fi
          (gh run download -n image-security-scan-develop-result -R ${{ github.repository }} 2>&1 >/dev/null) || :
          if [[ -e ./image-trivy.json ]]; then
            mv ./image-trivy.json ./image-trivy-develop.json
          else
            echo "null" > ./image-trivy-develop.json
          fi

      - name: Analyse the image diff result
        if: >-
          github.ref != 'refs/heads/develop' && 
          github.head_ref != 'security-scan-ee'
        id: set-diff-matrix
        run: | 
          result=$(docker run --rm -v /home/runner/work/portainer/portainer:/data oscarzhou/scan-report:0.1.4 diff -report-type=trivy -path="/data/image-trivy-feature.json" -compare-to="./data/image-trivy-develop.json" -output-type=matrix)
          echo "::set-output name=image_diff_result::${result}"

  result-analysis-compared-to-develop:
    name: Analyse scan result compared to develop
    # needs: [client-dependencies, server-dependencies, image-vulnerability]
    needs: [image-vulnerability]
    runs-on: ubuntu-latest
    if: >-
      github.ref != 'refs/heads/develop' && 
      github.head_ref != 'security-scan-ee'
    strategy:
      matrix: 
        # jsdiff: ${{fromJson(needs.client-dependencies.outputs.jsdiff)}}
        # godiff: ${{fromJson(needs.client-dependencies.outputs.godiff)}}
        imagediff: ${{fromJson(needs.client-dependencies.outputs.imagediff)}}
    steps:
      # - name: Display the results of js, go and image
      #   run: |
      #     echo ${{ matrix.jsdiff.summary }}
      #     echo ${{ matrix.godiff.summary }}
      #     echo ${{ matrix.imagediff.summary }}

      - name: Check job status of diff result
        # if: >-
        #   github.ref != 'refs/heads/develop' &&
        #   github.head_ref != 'security-scan-ee' && 
        #   (matrix.jsdiff.status == 'failure' || 
        #   matrix.godiff.status == 'failure' || 
        #   matrix.imagediff.status == 'failure')
        if: >-
          github.ref != 'refs/heads/develop' &&
          github.head_ref != 'security-scan-ee' && 
          matrix.imagediff.status == 'failure' 
        run: |
          echo ${{ matrix.imagediff.summary }}
          exit 1

  result-analysis:
    name: Analyse scan result
    # needs: [client-dependencies, server-dependencies, image-vulnerability]
    needs: [image-vulnerability]
    runs-on: ubuntu-latest
    if: >-
      github.ref == 'refs/heads/develop' ||
      github.head_ref == 'security-scan-ee'
    strategy:
      matrix: 
        # js: ${{fromJson(needs.client-dependencies.outputs.js)}}
        # go: ${{fromJson(needs.server-dependencies.outputs.go)}}
        image: ${{fromJson(needs.server-dependencies.outputs.image)}}
    steps:
      - name: Display the results of js, go and image
        run: |
          echo ${{ matrix.js.summary }}
          echo ${{ matrix.go.summary }}
          echo ${{ matrix.image.summary }}

      - name: Post the result to a Slack channel
        if: >- 
          (github.ref == 'refs/heads/develop' ||
          github.head_ref == 'security-scan-ee') && 
          (matrix.js.status == 'failure' ||
          matrix.go.status == 'failure')
        uses: slackapi/slack-github-action@v1.18.0
        with:
          # Slack channel id, channel name, or user id to post message.
          # See also: https://api.slack.com/methods/chat.postMessage#channels
          channel-id: 'C039ZB872N5'
          # For posting a rich message using Block Kit
          payload: |
            {
              "text": "Code Scanning Result:\nJS dependency check: ${{ matrix.js.status }} ${{ matrix.js.summary }}\nGo dependency check: ${{ matrix.go.status }} ${{ matrix.go.summary\nImage vulnerability check: ${{ matrix.image.status }} ${{ matrix.image.summary }}",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "Code Scanning Result:\nJS dependency check: ${{ matrix.js.status }} ${{ matrix.js.summary }}\nGo dependency check: ${{ matrix.go.status }} ${{ matrix.go.summary\nImage vulnerability check: ${{ matrix.image.status }} ${{ matrix.image.summary }}"
                  }
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}


